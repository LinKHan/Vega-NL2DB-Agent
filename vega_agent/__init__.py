"""Vega NL2DB Agent package.

This package contains the production-style split of the original
``baseline_3.py`` demo.  Modules are grouped by responsibility so the Agent
planner, schema retrieval, database execution, rendering, and UI can evolve
independently while keeping the same external behavior.
"""

